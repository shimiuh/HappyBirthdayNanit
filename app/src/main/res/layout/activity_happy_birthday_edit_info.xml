<?xml version="1.0" encoding="utf-8"?>
    <layout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        tools:context=".view.HappyBirthdayEditInfoActivity"
        >

    <data>
        <import type="android.view.View"/>
        <import type="android.text.TextUtils"/>
        <variable
            name="userViewModel"
            type="app.nanit.com.happybirthdaynanit.viewmodel.UserViewModel" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="8dp"
        android:paddingTop="30dp"
        android:orientation="vertical"
        android:gravity="center_horizontal">

        <EditText
            android:id="@+id/edit_info_user_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingTop="20dp"
            android:paddingBottom="20dp"
            android:hint="@string/name_hint"
            android:addTextChangedListener="@{userViewModel.mWatcher}"
            android:inputType="textPersonName"/>

        <TextView
            android:id="@+id/edit_info_birthday"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingTop="20dp"
            android:paddingBottom="20dp"
            android:setOnFocusChangeListener="@{userViewModel.mRemoveKeyboard}"
            android:onClick="@{userViewModel.mOnSetBirthdayListener}"
            style="@android:style/Widget.EditText"
            android:hint="@string/birthday_hint"/>

        <FrameLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content">

            <ImageView
                android:layout_width="200dp"
                android:layout_height="200dp"
                android:layout_marginTop="20dp"
                android:src="@drawable/default_place_holder_blue"/>

            <ImageView
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_gravity="end"
                android:layout_marginTop="50dp"
                android:src="@drawable/camera_icon_blue"/>

        </FrameLayout>

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="30dp"
            android:onClick="@{userViewModel.mShowBirthdayScreen}"
            android:text="@string/show_birthday"/>

    </LinearLayout>
</layout>